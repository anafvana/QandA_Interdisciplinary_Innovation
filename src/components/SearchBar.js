//import './SearchBar.css'

const SearchBar = (props) => {
    return (
        <div className="searchBarContainer">
            <h6 className="propTitle searchTitle">Search anything</h6>
            <input className="searchBar"/>
        </div>
    );
}

export default SearchBar;